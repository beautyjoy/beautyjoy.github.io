test:
  override:
      - bundle exec jekyll build -V -t --config _config.yml _config_staging.yml
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync _site s3://aws-website-bjc-website-cjgl5 --delete